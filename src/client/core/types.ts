import { pid_t } from "../../common/types"

export type screen_pos_t = { x: number, y: number };
export type screen_size_t = { w: number, h: number };
export type element_size_t = { w: number, h: number };

/*
export type search_result_t = {
    pid: pid_t,
}
*/
